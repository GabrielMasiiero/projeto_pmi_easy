<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buscar Tatuadores - Easy Tattoo</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/sidebar.css">
    <link rel="stylesheet" href="/css/tema.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <style>
        body {
            overflow-x: hidden;
        }

        .search-container {
            margin-left: 100px;
            margin-right: 20px;
            padding: 20px;
        }

        @media (max-width: 768px) {
            .search-container {
                margin-left: 20px;
            }
        }

        .search-box {
            background-color: #2e2e2e;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 30px;
        }

        .search-box h2 {
            color: #4fc3f7;
            margin-bottom: 20px;
        }

        .search-form {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
        }

        .search-form input,
        .search-form select {
            padding: 10px;
            background-color: #1c1c1c;
            border: 1px solid #444;
            border-radius: 5px;
            color: #e0e0e0;
        }

        .search-form button {
            padding: 10px 20px;
            background-color: #4fc3f7;
            color: #1c1c1c;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: 600;
        }

        .search-form button:hover {
            background-color: #29b6f6;
        }

        .results-container {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 20px;
        }

        .artist-card {
            background-color: #2e2e2e;
            padding: 20px;
            border-radius: 10px;
            transition: transform 0.3s;
        }

        .artist-card:hover {
            transform: translateY(-5px);
        }

        .artist-header {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-bottom: 15px;
        }

        .artist-avatar {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background-color: #4fc3f7;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            color: #1c1c1c;
            font-weight: 600;
        }

        .artist-info h3 {
            color: #4fc3f7;
            margin-bottom: 5px;
        }

        .artist-location {
            color: #888;
            font-size: 0.9em;
        }

        .artist-bio {
            color: #ccc;
            margin-bottom: 15px;
            line-height: 1.5;
        }

        .artist-styles {
            display: flex;
            flex-wrap: wrap;
            gap: 5px;
            margin-bottom: 15px;
        }

        .style-tag {
            background-color: #3a3a3a;
            color: #4fc3f7;
            padding: 4px 10px;
            border-radius: 12px;
            font-size: 0.8em;
        }

        .view-profile-btn {
            display: block;
            text-align: center;
            padding: 10px;
            background-color: #444;
            color: #e0e0e0;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .view-profile-btn:hover {
            background-color: #555;
        }

        .no-results {
            grid-column: 1 / -1;
            text-align: center;
            padding: 40px;
            color: #888;
        }
    </style>
</head>

<body>

    <!-- Sidebar -->
    <div class="sidebar" id="sidebar">
        <div class="sidebar-header">
            <span class="menu-icon">☰</span>
            <h2 class="header-text">Easy Tattoo</h2>
        </div>

        <ul class="sidebar-menu">
            <li><a href="/perfil"><i class="fa fa-user"></i><span class="menu-text">Meu Perfil</span></a></li>
            <li><a href="/posts"><i class="fa fa-paint-brush"></i><span class="menu-text">Feed</span></a></li>
            <li><a href="/posts/criar"><i class="fa fa-plus"></i><span class="menu-text">Criar Post</span></a></li>
            <li><a href="/user/busca"><i class="fa fa-search"></i><span class="menu-text">Buscar Tatuadores</span></a>
            </li>
            <li><a href="/sobre"><i class="fa fa-info-circle"></i><span class="menu-text">Sobre</span></a></li>
        </ul>

        <div class="sidebar-footer">
            <a href="/config"><i class="fa fa-cogs"></i><span class="menu-text">Configurações</span></a>
            <a href="/logout"><i class="fa fa-sign-out-alt"></i><span class="menu-text">Logout</span></a>
        </div>
    </div>

    <div class="search-container">
        <div class="search-box">
            <h2>🔍 Buscar Tatuadores</h2>
            <form class="search-form" method="GET" action="/user/busca">
                <input type="text" name="query" placeholder="Nome do tatuador..." value="<%= query || '' %>">
                <input type="text" name="cidade" placeholder="Cidade..." value="<%= cidade || '' %>">
                <select name="estado">
                    <option value="">Todos os Estados</option>
                    <option value="SP" <%=estado==='SP' ? 'selected' : '' %>>São Paulo</option>
                    <option value="RJ" <%=estado==='RJ' ? 'selected' : '' %>>Rio de Janeiro</option>
                    <option value="MG" <%=estado==='MG' ? 'selected' : '' %>>Minas Gerais</option>
                    <option value="PR" <%=estado==='PR' ? 'selected' : '' %>>Paraná</option>
                    <option value="RS" <%=estado==='RS' ? 'selected' : '' %>>Rio Grande do Sul</option>
                    <option value="SC" <%=estado==='SC' ? 'selected' : '' %>>Santa Catarina</option>
                    <option value="BA" <%=estado==='BA' ? 'selected' : '' %>>Bahia</option>
                    <option value="PE" <%=estado==='PE' ? 'selected' : '' %>>Pernambuco</option>
                    <option value="CE" <%=estado==='CE' ? 'selected' : '' %>>Ceará</option>
                    <option value="GO" <%=estado==='GO' ? 'selected' : '' %>>Goiás</option>
                </select>
                <select name="estilos">
                    <option value="">Todos os Estilos</option>
                    <option value="tradicional" <%=estilos==='tradicional' ? 'selected' : '' %>>Tradicional</option>
                    <option value="old-school" <%=estilos==='old-school' ? 'selected' : '' %>>Old School</option>
                    <option value="realismo" <%=estilos==='realismo' ? 'selected' : '' %>>Realismo</option>
                    <option value="aquarela" <%=estilos==='aquarela' ? 'selected' : '' %>>Aquarela</option>
                    <option value="blackwork" <%=estilos==='blackwork' ? 'selected' : '' %>>Blackwork</option>
                    <option value="geometrico" <%=estilos==='geometrico' ? 'selected' : '' %>>Geométrico</option>
                    <option value="minimalista" <%=estilos==='minimalista' ? 'selected' : '' %>>Minimalista</option>
                    <option value="japones" <%=estilos==='japones' ? 'selected' : '' %>>Japonês</option>
                    <option value="tribal" <%=estilos==='tribal' ? 'selected' : '' %>>Tribal</option>
                </select>
                <button type="submit">Buscar</button>
            </form>
        </div>

        <div class="results-container">
            <% if (tatuadores && tatuadores.length> 0) { %>
                <% tatuadores.forEach(artist=> { %>
                    <div class="artist-card">
                        <div class="artist-header">
                            <div class="artist-avatar">
                                <%= artist.nome.charAt(0).toUpperCase() %>
                            </div>
                            <div class="artist-info">
                                <h3>
                                    <%= artist.nome %>
                                </h3>
                                <p class="artist-location">
                                    <i class="fa fa-map-marker-alt"></i>
                                    <%= artist.cidade || 'Cidade não informada' %>, <%= artist.estado || 'UF' %>
                                </p>
                            </div>
                        </div>

                        <% if (artist.bio) { %>
                            <p class="artist-bio">
                                <%= artist.bio %>
                            </p>
                            <% } %>

                                <% if (artist.estilos && artist.estilos.length> 0) { %>
                                    <div class="artist-styles">
                                        <% artist.estilos.forEach(estilo=> { %>
                                            <span class="style-tag">
                                                <%= estilo %>
                                            </span>
                                            <% }); %>
                                    </div>
                                    <% } %>

                                        <a href="/user/perfil/<%= artist._id %>" class="view-profile-btn">Ver Perfil</a>
                    </div>
                    <% }); %>
                        <% } else { %>
                            <div class="no-results">
                                <p>Nenhum tatuador encontrado com esses critérios.</p>
                            </div>
                            <% } %>
        </div>
    </div>

    <script src="/js/script.js"></script>

</body>

</html>