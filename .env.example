# Configuração do Servidor
PORT=3000
NODE_ENV=development

# MongoDB
# Para desenvolvimento local com Docker:
MONGO_URI=mongodb://admin:admin123@localhost:27017/EasyTattoDB?authSource=admin

# Para MongoDB Atlas (produção):
# MONGO_URI=mongodb+srv://<usuario>:<senha>@cluster.mongodb.net/EasyTattoDB?retryWrites=true&w=majority

# Sessão
# IMPORTANTE: Gere uma chave secreta forte para produção
# Você pode gerar uma usando: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-secret-key-change-in-production-please

# Upload de arquivos
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/jpg,image/webp
